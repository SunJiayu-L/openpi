# 工作总结（2026-03-28）

## 1) 数据与训练配置改动

我修改了 `src/openpi/training/config.py`，新增/调整了 3 个单任务训练配置，基于 `libero_10` 的任务索引：

- `pi05_libero10_task0_retrain`
- `pi05_libero10_task6_retrain`
- `pi05_libero10_task9_retrain`

关键点：

- `weight_loader` 全部改为从 10k 步加载：
  - `/storage/yukaichengLab/lishiwen/jiayusun/openpi/checkpoints/pi05_libero_RETRAIN_base/pi05_libero_RETRAIN_base/10000/params`
- 每个配置都用了独立 `asset_id`，避免 norm stats 互相覆盖。
- 训练数据都来自 `libero_10` 对应 episode 子集（从本地 `huggingface/lerobot` 解析得到）。

## 2) 评测脚本改动

我修改了 `eval_pi05_sgo_lm90_all_suites_2gpu.sbatch` 的 checkpoint 路径：

- 旧路径（不存在）：`checkpoints/pi05_libero_sgo_lm90/.../29999`
- 新路径（你重命名后）：`/storage/yukaichengLab/lishiwen/jiayusun/openpi/checkpoints/pi05_libero_RETRAIN_base/pi05_libero_RETRAIN_base/29999`

## 3) 作业提交与状态（训练）

### 先前作业（已取消）

- `32596`, `32597`, `32598`（旧任务组合，已取消）
- `32599`, `32600`（改投前中间作业，已取消）
- `32603`（PENDING，后改投 031，已取消）

### 当前有效训练作业（都在 RUNNING）

- `32601`：`pi05_libero10_task0_retrain`，节点 `gnho034`
- `32602`：`pi05_libero10_task6_retrain`，节点 `gnho009`
- `32607`：`pi05_libero10_task9_retrain`，节点 `gnho031`

## 4) 作业提交与状态（评测）

### 失败的第一次评测

- `32595`：`FAILED`
- 原因：第二轮启动 server 时找不到 checkpoint 路径（`FileNotFoundError`）。
- 该次只完成了前两套（`libero_goal`, `libero_10`），后两套未跑起。

### 修正后的评测

- `32605`：`COMPLETED`（节点 `gnho031`）
- 全部 4 个 suite 跑完（每 suite 每 task 20 次）

结果（每个 suite 总计 200 episodes）：

- `libero_goal`: `0.98`
- `libero_10`: `0.0`
- `libero_object`: `0.995`
- `libero_spatial`: `0.985`

### 重复评测作业处理

- `32604`（挂在 034 的重复评测）已取消。

## 5) 备注

- 评测日志中有 `libGLU.so.0 / EGL_NOT_INITIALIZED` 的 `Exception ignored`，出现在进程退出清理阶段；`32605` 结果不受影响（最终状态 `COMPLETED`）。
